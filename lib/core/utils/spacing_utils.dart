import 'package:flutter/material.dart';

Widget verticalSpace({required double space}) => SizedBox(
      height: space,
    );

Widget horizontalSpace({required double space}) => SizedBox(
      width: space,
    );
